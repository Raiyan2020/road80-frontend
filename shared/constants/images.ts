export const APP_LOGO_URL = '/road-logo.png';

/** Logo variant burned into ad photos on upload. Kept separate from
 *  APP_LOGO_URL so the watermark can be retuned without touching app chrome. */
export const WATERMARK_LOGO_URL = '/road-logo-watermark.png';

export const FALLBACK_LISTING_IMAGE = APP_LOGO_URL;
